debug: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/
server:
   port: 9004
spring:
    application:
            name: test-service
    cloud:
      config:
        profile: dev
        discovery:
          enabled: true
          service-id: spring-cloud-config-server

