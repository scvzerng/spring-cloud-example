eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/
server:
   port: 9005
spring:
    application:
            name: hystrix-dashboard
    cloud:
      config:
        profile: dev
        discovery:
          enabled: true
          service-id: spring-cloud-config-server

